#include <stdio.h>
int main()
{
    int a,e,res=1,ed;
    printf("Enter the base and exponent values:");
    scanf("%d %d",&a,&e);
    ce=e;
    while(e>0)
    {
        res=res*b;
        e--;
    }
    printf("%d power %d is:%d",a,ed,res);
    return 0;
}
